<h2>投稿一覧</h2>
<%= link_to '新規投稿へ', new_post_path %>

<div class="post_index">
    <% @posts.each do |p| %>
        <div class="post">
            テーマ：<%= p.theme %>
        </div>
    <% end %>
</div>
